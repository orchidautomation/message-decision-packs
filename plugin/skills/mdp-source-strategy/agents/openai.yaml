interface:
  display_name: "MDP Source Strategy"
  short_description: "Plan source targets and evidence strategy"
  default_prompt: "Use $mdp-source-strategy to create a reviewed mdp.source-strategy.v0 artifact with an agent operating plan, provider-specific search/extraction prompts, evidence gates, exclusions, and downstream MDP handoff instructions before extraction, GTM scouting, or proposal source intake."
