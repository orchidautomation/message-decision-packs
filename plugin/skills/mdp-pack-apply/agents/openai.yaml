interface:
  display_name: "MDP Pack Apply"
  short_description: "Apply a job through verified requirements"
  default_prompt: "Use $mdp-pack-apply to verify or compile exact-job requirements, collect only missing evidence, and apply the selected job through verified final validators without editing the pack."
