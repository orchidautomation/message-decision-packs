# Archived deployable experiments

These examples are kept for historical implementation context only. They are not the active turnkey path.

Active deployable template:

- `examples/ai-sdr-eve-vercel/` — Eve-native Vercel scout with MDP source strategy, Cron, Exa person-resolution, MDP fit/brief gates, and a Deploy to Vercel button.

Archived here:

- `mdp-bdr-scout-vercel/` — earlier Next.js/Workflow-style BDR scout scaffold.
- `flue-webhook-agent/` — earlier Flue webhook draft-response adapter.

Prefer the Eve template for new demos, docs, releases, and operator onboarding.
