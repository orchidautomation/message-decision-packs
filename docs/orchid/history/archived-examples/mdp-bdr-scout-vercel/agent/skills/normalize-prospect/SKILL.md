# Normalize Prospect Skill

Use when converting public evidence into an MDP-compatible candidate row.

Required fields: name or account identifier, title or role when person-level, company, source kind, trigger, and evidence IDs.

Do not infer private contact details. Leave uncertain fields null and reduce operational confidence.
